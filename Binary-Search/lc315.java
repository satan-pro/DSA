import java.util.*;
class lc315
{
	public List<Integer> countSmaller(int[] nums) {
        // create hashmap of og nums array
        // sort og array
        // another hashmap to store number that is filled in the index
        HashMap<Integer, Integer> pos = new HashMap<>();

        for(int i=0; i<arr.length; i++)
        {
        	
        }
    }
}